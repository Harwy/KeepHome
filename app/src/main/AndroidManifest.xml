<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shu.keephome">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 获取WIFI状态权限 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 获取网络状态权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 开机自启权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />


    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_keephome"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_keephome"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".DrawableActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DataShowActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".DeviceAddActivity" />
        <activity android:name=".InfActivity" />
        <activity android:name=".AddActivity" >

        </activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".ForgetActivity" />
        <!-- Mqtt Service -->
        <service
            android:name=".service.AutoUpdateService"
            android:enabled="true"
            android:exported="true"
            android:process=":AutoUpdateService">
            <intent-filter>
                <action android:name="com.shu.service" />
            </intent-filter>
        </service>

        <!-- 广播接收器 -->
        <receiver android:name=".service.MyBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </receiver>

        <activity android:name=".ControlActivity">
            <intent-filter>
                <action android:name="com.shu.keephome.CONTROL"/>
            </intent-filter>
        </activity>
    </application>

</manifest>